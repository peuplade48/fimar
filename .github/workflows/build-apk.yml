name: 🔨 Apartman Takip APK Build

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:
    inputs:
      build_type:
        description: 'Build türü seçin'
        required: true
        default: 'debug'
        type: choice
        options: [debug, release]
      server_url:
        description: 'Sunucu URL'
        required: false
        default: ''

# 1. KRİTİK AYAR: Yazma izni (Tag oluşturabilmek için)
permissions:
  contents: write

env:
  JAVA_VERSION: '17'

jobs:
  build:
    name: 📦 APK Oluştur
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Kodu İndir
        uses: actions/checkout@v4
        with:
          fetch-depth: 0 # 2. KRİTİK AYAR: Eski tagleri görebilmesi için şart

      - name: ☕ JDK Kur
        uses: actions/setup-java@v4
        with:
          java-version: ${{ env.JAVA_VERSION }}
          distribution: 'temurin'
          cache: gradle

      # ── 1. ADIM: PROJE YAPISINI KONTROL ET ───────────────────────────
      - name: 🛠️ Klasör Kontrolü
        run: |
          if [ -d "android" ]; then
            cp -r android/* .
            rm -rf android
          fi

      # ── 2. ADIM: GRADLE HAZIRLA ──────────────────────────────────────
      - name: 🛠️ Gradle Wrapper Hazırla
        run: |
          rm -rf gradle/ gradlew gradlew.bat
          gradle wrapper --gradle-version 8.7
          chmod +x ./gradlew

      # ── 3. ADIM: URL GÜNCELLEME ──────────────────────────────────────
      - name: 🌐 Sunucu URL Güncelle
        if: github.event.inputs.server_url != ''
        run: |
          SERVER="${{ github.event.inputs.server_url }}"
          find . -name "MainActivity.kt" -exec sed -i "s|private val SERVER_URL = \".*\"|private val SERVER_URL = \"$SERVER\"|g" {} +

     # ── 4. ADIM: OTOMATİK VERSİYON ARTIRMA (Daha Güvenli Hali) ────────
      - name: 🏷️ Yeni Versiyon Belirle ve Tag Oluştur
        id: auto_tag
        run: |
          # En son tag'i al, yoksa 1.0.0 (v olmadan)
          LATEST_TAG=$(git describe --tags --abbrev=0 2>/dev/null | sed 's/v//g' || echo "1.0.0")
          
          # Versiyonu parçala
          IFS='.' read -r MAJOR MINOR PATCH <<< "$LATEST_TAG"
          NEW_PATCH=$((PATCH + 1))
          NEW_TAG="$MAJOR.$MINOR.$NEW_PATCH"
          
          echo "new_tag=$NEW_TAG" >> $GITHUB_OUTPUT
          
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git tag "v$NEW_TAG" # GitHub'da v1.0.1 görünsün
          git push origin "v$NEW_TAG"
          
      # ── 5. ADIM: KEYSTORE HAZIRLA ────────────────────────────────────
      - name: 🔑 Release Keystore Hazırla
        env:
          MY_KEYSTORE_SECRET: ${{ secrets.KEYSTORE_BASE64 }}
        if: (github.event.inputs.build_type == 'release' || steps.auto_tag.outputs.new_tag != '') && env.MY_KEYSTORE_SECRET != ''
        run: |
          mkdir -p app
          echo "$MY_KEYSTORE_SECRET" | base64 --decode > app/release-key.jks

      # ── 6. ADIM: BUILD (Dinamik Versiyon Gömme) ──────────────────────
      - name: 🛠️ APK Build
        run: |
          BUILD_TYPE="${{ github.event.inputs.build_type || 'debug' }}"
          # 1.0.1 -> 1.0.1 (v harfini temizle)
          CLEAN_VER=$(echo "${{ steps.auto_tag.outputs.new_tag }}" | sed 's/v//g')
          
          if [ "$BUILD_TYPE" == "release" ]; then
            ./gradlew assembleRelease --no-daemon \
              -Pandroid.injected.versionName="$CLEAN_VER" \
              -PKEYSTORE_FILE="release-key.jks" \
              -PKEYSTORE_PASS="${{ secrets.KEYSTORE_PASSWORD }}" \
              -PKEY_ALIAS="${{ secrets.KEY_ALIAS }}" \
              -PKEY_PASS="${{ secrets.KEY_PASSWORD }}" \
              -Porg.gradle.jvmargs="-Xmx3g"
          else
            ./gradlew assembleDebug --no-daemon \
              -Pandroid.injected.versionName="$CLEAN_VER" \
              -Porg.gradle.jvmargs="-Xmx3g"
          fi

      # ── 7. ADIM: ARTIFACTS ──────────────────────────────────────────
      - name: 📤 APK Geçici Kaydet
        uses: actions/upload-artifact@v4
        with:
          name: apartman-takip-${{ steps.auto_tag.outputs.new_tag }}
          path: "app/build/outputs/apk/**/*.apk"

      # ── 8. ADIM: GITHUB RELEASE ─────────────────────────────────────
      - name: 🚀 GitHub Release Oluştur
        uses: softprops/action-gh-release@v2
        with:
          files: |
            app/build/outputs/apk/release/*.apk
            app/build/outputs/apk/debug/*.apk
          name: "Apartman Takip Sürüm ${{ steps.auto_tag.outputs.new_tag }}"
          tag_name: ${{ steps.auto_tag.outputs.new_tag }}
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
