name: 🔨 Apartman Takip APK Build

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:
    inputs:
      build_type:
        description: 'Build türü'
        required: true
        default: 'debug'
        type: choice
        options: [debug, release]

permissions:
  contents: write

jobs:
  build:
    name: 📦 APK Oluştur
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Kodu İndir
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: ☕ JDK Kur
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: gradle

      # ── 1. ADIM: VERSİYON BELİRLE ────────────────────────────────────
      - name: 🏷️ Versiyonu Belirle
        id: versioning
        run: |
          # Build numarasını kullanarak 1.0.X formatında versiyon oluşturur
          V_NAME="1.0.${{ github.run_number }}"
          V_CODE="${{ github.run_number }}"
          echo "v_name=$V_NAME" >> $GITHUB_OUTPUT
          echo "v_code=$V_CODE" >> $GITHUB_OUTPUT
          
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git tag "$V_NAME" || echo "Tag zaten var."
          git push origin "$V_NAME" || echo "Push yapıldı."

      # ── 2. ADIM: GRADLE VE DOSYA HAZIRLIĞI ──────────────────────────
      - name: 🛠️ Dosyaları Hazırla
        run: |
          if [ -d "android" ]; then cp -r android/* .; rm -rf android; fi
          
          # build.gradle dosyasındaki etiketleri GERÇEK değerlerle değiştiriyoruz
          # Bu yöntem asla sekmez çünkü __VCODE__ ve __VNAME__ eşsizdir.
          sed -i "s/__VCODE__/${{ steps.versioning.outputs.v_code }}/g" app/build.gradle
          sed -i "s/__VNAME__/${{ steps.versioning.outputs.v_name }}/g" app/build.gradle
          
          # Gradle Wrapper'ı tamir et
          gradle wrapper --gradle-version 8.7
          chmod +x ./gradlew
          
          echo "Dosya Güncellendi. Kontrol:"
          grep -E "versionCode|versionName" app/build.gradle

      # ── 3. ADIM: APK BUILD ──────────────────────────────────────────
      - name: 🛠️ APK Build
        run: |
          TYPE="${{ github.event.inputs.build_type || 'debug' }}"
          VARIANT=$(echo "$TYPE" | awk '{print toupper(substr($0,1,1))substr($0,2)}')
          ./gradlew clean assemble$VARIANT --no-daemon

      # ── 4. ADIM: RELEASE ───────────────────────────────────────────
      - name: 🚀 GitHub Release Oluştur
        uses: softprops/action-gh-release@v2
        with:
          files: "app/build/outputs/apk/**/*.apk"
          name: "Apartman Takip v${{ steps.versioning.outputs.v_name }}"
          tag_name: ${{ steps.versioning.outputs.v_name }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
